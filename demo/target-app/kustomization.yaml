# ABOUTME: Kustomize configuration for demo target application
# ABOUTME: ArgoCD deploys this in sync wave 5

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: demo

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/part-of: sovereign-compliance-demo
  app.kubernetes.io/managed-by: argocd
